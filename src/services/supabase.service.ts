import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class SupabaseService {
  // Este servicio se ha dejado vacío intencionadamente después de eliminar la integración con Supabase.
  // Puede ser utilizado para una futura integración de backend.
  constructor() {}
}
